{{ 'testimonials.css' | asset_url | stylesheet_tag }}

{%- style -%}
{%- endstyle -%}

<section class="testimonials">
  <div class="testimonials__heading">
    <h6>Recent reviews</h6>
  </div>
  <div class="testimonials__content">
    <div class="testimonials__row">
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--right">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% else %}

          {% endif %}
        {% endfor -%}
      </div>
    </div>
    <div class="testimonials__row--reversed">
      <div class="testimonials__slide--left">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
          {% else %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--left">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
          {% else %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--left">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
          {% else %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--left">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
          {% else %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--left">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
          {% else %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--left">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
          {% else %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--left">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
          {% else %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% endif %}
        {% endfor -%}
      </div>
      <div class="testimonials__slide--left">
        {% for block in section.blocks %}
          {% assign num = forloop.index | modulo: 2 %}
          {% if num == 0 %}
          {% else %}
            <div class="testimonials__item">
              <h6>{{ block.settings.text }}</h6>
            </div>
          {% endif %}
        {% endfor -%}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Testimonials",
  "settings": [],
  "max_blocks": 20,
  "blocks": [
    {
      "type": "text",
      "name": "text",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Text"
        }
      ]
    }
  ],
  "presets": []
}
{% endschema %}

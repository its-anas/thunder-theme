{{ 'multi-icon.css' | asset_url | stylesheet_tag }}

{% liquid
  assign mobile_items = 3
  assign tablet_items = 6
  assign desktop_items = 8
%}

{%- style -%}
   .multi-icon {
    --mobile-small-item-width: calc(var(--slider-350-width) / {{ mobile_items }} * 1px);
    --mobile-item-width: calc(var(--slider-350-750-width) / {{ mobile_items }} * 1px);
    --tablet-item-width: calc(var(--slider-750-1024-width) / {{ tablet_items }} * 1px);
    --desktop-item-width: calc(var(--slider-1024-width) / {{ desktop_items }} * 1px);
   }

  .{{ section.id }}.multi-icon {
    background-color: {{ section.settings.background_color }};
    padding: {{ section.settings.space_outside_elements | divided_by: 2 }}px 0;
  }


  @media screen and (max-width: 350px) {
    .multi-icon .item {
      width: var(--mobile-small-item-width);
      max-width: var(--mobile-small-item-width);
      min-width: var(--mobile-small-item-width);
    }
  }
  @media screen and (min-width: 350px) and (max-width: 750px) {
    .multi-icon .item {
      width: var(--mobile-item-width);
      max-width: var(--mobile-item-width);
      min-width: var(--mobile-item-width);
    }
  }
  @media screen and (min-width: 750px) and (max-width: 1024px) {
    .multi-icon .item {
      width: var(--tablet-item-width);
      max-width: var(--tablet-item-width);
      min-width: var(--tablet-item-width);
    }
  }
  @media screen and (min-width: 1024px) {
    .multi-icon .item {
      width: var(--desktop-item-width);
      max-width: var(--desktop-item-width);
      min-width: var(--desktop-item-width);
    }
  }
{%- endstyle -%}

<slider-section
  data-mobile-items="{{ mobile_items }}"
  data-tablet-items="{{ tablet_items }}"
  data-desktop-items="{{ desktop_items }}"
  class="{{ section.id }} multi-icon {% if section.settings.section_width == "wrapped" %}wrapped{% else %}{% endif %}"
>
  {% if section.settings.heading != blank %}
    <div class="multi-icon__heading">
      <h6>{{ section.settings.heading }}</h6>
    </div>
  {% endif %}
  <div class="slider-component">
    <div class="prev">
      {% render 'icons', icon: 'chevron-prev' -%}
    </div>
    <div class="slides-container">
      <div class="slide">
        {% for block in section.blocks %}
          <div class="item" {{ block.shopify_attributes }}>
            <a href="{{ block.settings.url }}" class="multi-icon__icon media">
              {% if block.settings.icon_type == 'svg' %}
                {% if block.settings.svg == blank %}
                  {% render 'icons', icon: 'multi-icon' %}
                {% else %}
                  {{ block.settings.svg }}
                {% endif %}
              {% elsif block.settings.icon_type == 'image' %}
                {% if block.settings.image == blank %}
                  {{ 'product-1' | placeholder_svg_tag: 'placeholder' }}
                {% else %}
                  <img
                    srcset="{{ block.settings.image | image_url }}"
                    loading="lazy"
                    alt="{{ block.settings.image | escape }}"
                    width="{{ block.settings.image.width }}"
                    height="{{ block.settings.image.height }}"
                    class="full"
                  >
                {% endif %}
              {% endif %}
            </a>
            {% if block.settings.text != blank %}
              <p class="multi-icon__text">{{ block.settings.text }}</p>
            {% endif %}
          </div>
        {% endfor %}
      </div>
    </div>
    <div class="next">
      {% render 'icons', icon: 'chevron-next' -%}
    </div>
  </div>
</slider-section>

{% schema %}
{
  "name": "Multi icon",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Filter by"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#222222"
    },
    {
      "type": "range",
      "id": "space_outside_elements",
      "label": "Space outside elements",
      "min": 30,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 50
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "info": "Desktop only",
      "options": [
        {
          "value": "full-width",
          "label": "Full width"
        },
        {
          "value": "wrapped",
          "label": "Wrapped"
        }
      ],
      "default": "full-width"
    }
  ],
  "blocks": [
    {
      "type": "image_picker",
      "name": "Icon",
      "settings": [
        {
          "type": "url",
          "id": "url",
          "label": "Direction URL"
        },
        {
          "type": "select",
          "id": "icon_type",
          "label": "Icon type",
          "options": [
            {
              "value": "svg",
              "label": "svg"
            },
            {
              "value": "image",
              "label": "Image"
            }
          ],
          "default": "svg",
          "info": "It is recommended to use svg icons instead of jpeg/png icons to get the best quality"
        },
        {
          "type": "liquid",
          "id": "svg",
          "label": "Icon - svg",
          "info": "Applicable only if icon type is svg"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Icon - image",
          "info": "Applicable only if icon type is image"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Icon text",
          "default": "Icon"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Multi icon",
      "blocks": [
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
        }
      ]
    }
  ]
}
{% endschema %}
